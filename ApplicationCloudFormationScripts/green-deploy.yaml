apiVersion: apps/v1
kind: Deployment
metadata:
  name: ducks-deployment
spec:
  selector:
    matchLabels:
      app: green-ducks
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: ducks
    spec:
      containers:
        - name: ducks
          image: steeloctopus/duckhunt:latest
          ports:
            - containerPort: 80